require 'spec_helper'
require '<%= site_name.underscore %>'


# Pass :no_browser => true to not automatically invoke the browser
# for service tests for example
#  GoogleService.new(:no_browser => true) do |google_service|
#    google_service.blah
#  end


describe "<%= name.camelize %>Service" do
  it "should fail because you haven't written any tests yet" do
    false.should be_true
  end
end
