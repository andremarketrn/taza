<%= "#Generated at #{Time.now}" %>
$: << File.dirname(__FILE__)
require 'rubygems'
require 'taza/tasks'

Taza::Rake::Tasks.new do |t|
  file_hole = "artifacts/#{Time.now.to_i}"
  t.spec_opts = ["--format documentation --out output.txt"
  ]
end
