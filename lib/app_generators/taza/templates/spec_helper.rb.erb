ENV['TAZA_ENV'] = "integration" if ENV['TAZA_ENV'].nil?
require 'rubygems'
require 'rspec'
require 'mocha'

lib_path = File.expand_path("#{File.dirname(__FILE__)}/../lib/sites")
$LOAD_PATH.unshift lib_path unless $LOAD_PATH.include?(lib_path)

Rspec.configure do |c|
  #config here
end 